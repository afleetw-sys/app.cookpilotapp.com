# URL Import

Native source:
- `AddRecipeView.swift`
- browser app import flow

Key visual traits:
- import is a simple, trusted utility action
- input and button should feel like the same family as auth controls
- errors should appear as inline CookPilot state blocks, not default browser alerts

Browser primitives used:
- text input
- primary button
- state block
- content panel
